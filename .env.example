# Copy this file to .env.local for local development.
# Never commit .env.local to version control.

# NVIDIA Inference API key — required for the AI insights feature (Phase 6).
# Set this in Netlify → Site configuration → Environment variables for production.
NVIDIA_API_KEY=your_nvidia_api_key_here
